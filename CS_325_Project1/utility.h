/*  Fills an array from a file that contains integers in
    array format, i.e. [5, 4, -1,...]
    params:
        size:       the amount of numbers to be in the array
    returns:
        arr:        pointer to the array that was generated
*/
int *randomArray(int);
